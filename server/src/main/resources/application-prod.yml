spring:
  jpa:
    show-sql: false

cors:
  allowed-origins: https://ordovita.pl
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

logging:
  level:
    root: WARN
    pl.ordovita: INFO
    org.springframework.security: WARN
