**Card** — the flat Arena container; use to group related content. Defined by a 1px border, never a shadow (shadow is reserved for modals/menus/landing).

```jsx
<Card>
  <h3>Today</h3>
  <p>3 tasks due</p>
</Card>
```

Set `padded={false}` for list/media cards that manage their own insets.
