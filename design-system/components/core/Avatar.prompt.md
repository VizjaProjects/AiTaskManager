**Avatar** — initials in a neutral circle; use to represent a person (assignees, members, account). The Arena system uses initials, not photos.

```jsx
<Avatar name="Mateusz Barlik" />
<Avatar name="Alex Rivera" size="sm" />
```

Quiet `primary-fixed` background with ink initials; always `rounded-full`.
