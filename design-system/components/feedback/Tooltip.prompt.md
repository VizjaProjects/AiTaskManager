**Tooltip** — a small label on hover for icon-only affordances; wrap an `IconButton` or any trigger.

```jsx
<Tooltip label="Ask AI">
  <IconButton icon="auto_awesome" accent />
</Tooltip>
```

Uses the inverse surface (dark chip on light, light chip on dark) so it reads above any background.
