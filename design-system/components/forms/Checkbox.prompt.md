**Checkbox** — a small square toggle for multi-select / boolean options in forms and lists.

```jsx
<Checkbox checked={done} onChange={setDone} label="Mark complete" />
```

Checked fills with accent and shows a `check` glyph; unchecked is a bordered surface box. Radius stays `sm` (square), never `full`.
