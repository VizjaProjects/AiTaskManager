**Switch** — a pill toggle for binary settings (dark mode, workspace visibility).

```jsx
<Switch checked={isDark} onChange={setDark} />
```

On = accent track; off = neutral active track. The only `rounded-full` control besides pills/avatars.
